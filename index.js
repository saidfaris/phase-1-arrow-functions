function divide() {
    return 2000 / 100;
  }
  
  const square = (x) => x * x;
  
  const add = (x, y) => x + y;
  
  console.log(divide()); // 20
  console.log(square(5)); // 25
  console.log(add(10, 20)); // 30
  